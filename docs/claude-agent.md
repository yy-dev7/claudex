# Claude Agent 架构设计

## 为什么 CLI 必须在 Sandbox 中运行？

### 危险的架构（CLI 运行在后端容器）

```
❌ 危险的架构
┌─────────────────────────────────────────┐
│           后端容器                       │
│  ┌─────────────────────────────────┐    │
│  │      Claude Code CLI            │    │
│  │         ↓                       │    │
│  │  执行: rm -rf /app              │ ← 灾难！用户代码在你的服务器上运行
│  │  执行: cat /etc/passwd          │ ← 安全漏洞
│  │  执行: npm install malware      │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

### 正确的架构（Claudex 的做法）

```
✅ 安全的架构
后端容器                              E2B Sandbox（隔离环境）
┌──────────────┐                    ┌──────────────────────┐
│  SDK         │    控制信号         │   Claude Code CLI    │
│  (远程控制器) │ ──────────────────► │      ↓               │
│              │                    │   执行: rm -rf /     │ ← 只影响沙箱
│              │ ◄────────────────── │   执行: npm install  │ ← 隔离环境
│              │    响应流           │   执行: 任何代码     │
└──────────────┘                    └──────────────────────┘
```

## CLI 与 SDK 的关系

Claude Code CLI 和 Claude Code SDK **不是包含关系，而是独立的组件**，它们通过 Transport 层通信。

```
┌─────────────────┐           ┌─────────────────┐
│  Claude Code    │  控制/通信  │  Claude Code    │
│      SDK        │ ─────────► │      CLI        │
│  (Python 库)    │ ◄───────── │  (独立进程)      │
└─────────────────┘   响应流    └─────────────────┘
```

## SDK vs CLI 的区别

| 特性 | SDK | CLI |
|------|-----|-----|
| **本质** | Python 客户端库 | 完整的 AI Agent 可执行程序 |
| **安装** | `pip install claude-code-sdk` | `npm install -g @anthropic-ai/claude-code` |
| **职责** | 发送指令、接收响应、处理权限 | 调用 Claude API、执行工具（bash/文件等） |
| **依赖关系** | 需要 CLI 存在才能工作 | 可以独立运行（终端中直接使用） |

## SDK 的角色：远程控制器

SDK 通过 **Transport** 与 CLI 通信：

```python
# 本地运行 CLI
async with ClaudeSDKClient(options) as client:  # 默认 LocalTransport
    ...

# 远程 Sandbox 中运行 CLI
async with ClaudeSDKClient(options, transport=E2BSandboxTransport(...)) as client:
    ...
```

- **LocalTransport**: SDK 在本地启动 CLI 子进程
- **E2BSandboxTransport**: SDK 连接到远程 Sandbox 中已运行的 CLI

**SDK 是 CLI 的"遥控器"**，它本身不执行任何代码操作，只负责：
1. 启动或连接到 CLI
2. 发送用户 prompt
3. 接收 CLI 的流式响应
4. 处理权限请求等交互

**CLI 才是真正的"执行者"**，它：
1. 与 Claude API 通信获取指令
2. 执行 bash 命令、文件操作等工具
3. 将结果返回给 Claude API 继续对话

## 为什么后端容器也安装了 CLI？

后端容器安装 CLI 是为了 `enhance_prompt()` 这个辅助功能：

```python
# claude_agent.py 中的 enhance_prompt
async def enhance_prompt(self, prompt: str, model_id: str, user: User) -> str:
    options = ClaudeAgentOptions(
        max_turns=1,  # 只执行一轮
        permission_mode="bypassPermissions",  # 不需要权限
    )
    # 注意：这里没有传 transport，使用默认的本地 Transport
    async with ClaudeSDKClient(options=options) as client:
        await client.query(f"Enhance this prompt: {prompt}")
        ...
```

这个场景：
- 只是让 Claude 优化 prompt 文本
- 不需要执行任何代码/文件操作
- 单轮交互，快速返回
- 所以可以在后端容器本地运行

## 请求流程总结

```
┌─────────────────────────────────────────────────────────────┐
│  用户请求: "帮我创建一个 Node.js 项目"                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  后端 SDK: 把请求发送到 Sandbox 中的 CLI                      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Sandbox CLI:                                                │
│    1. 调用 Claude API                                        │
│    2. Claude 说: "执行 npm init -y"                          │
│    3. CLI 在 Sandbox 中执行 npm init -y  ← 必须有执行环境！    │
│    4. 把结果返回给 Claude API                                 │
│    5. Claude 说: "创建 index.js"                             │
│    6. CLI 在 Sandbox 中创建文件  ← 必须有文件系统！            │
│    7. ...                                                    │
└─────────────────────────────────────────────────────────────┘
```

**结论：CLI 必须在 Sandbox 中运行，因为它需要一个安全、隔离的环境来执行 Claude 决定要做的操作。**

## E2B Sandbox 数据持久化

### 问题：Sandbox 用完即销毁，产物如何保存？

E2B Sandbox 是临时性的隔离环境，会话结束后会被销毁。用户查看历史对话时，需要能够访问之前生成的代码和资源。

### 数据持久化策略

```
会话进行中                                    会话结束后
┌─────────────────────────────────────┐      ┌─────────────────────────────┐
│  E2B Sandbox                        │      │  持久化存储                  │
│  ┌─────────────────────────────┐    │      │  ┌───────────────────────┐  │
│  │  /workspace/                │    │      │  │  云存储 (S3/GCS)      │  │
│  │    ├── src/                 │────┼─────►│  │    └── 项目文件快照   │  │
│  │    ├── package.json         │    │      │  └───────────────────────┘  │
│  │    └── ...                  │    │      │  ┌───────────────────────┐  │
│  └─────────────────────────────┘    │      │  │  数据库               │  │
│                                     │      │  │    └── 对话历史       │  │
│  CLI 执行的所有操作都在这里          │      │  │    └── 文件元数据     │  │
└─────────────────────────────────────┘      │  └───────────────────────┘  │
         ↓ 销毁                              └─────────────────────────────┘
         🗑️                                            ↓ 用户查看历史
                                             ┌─────────────────────────────┐
                                             │  重新加载到新 Sandbox        │
                                             │  或直接从存储下载            │
                                             └─────────────────────────────┘
```

### 常见的持久化方案

| 方案 | 说明 | 适用场景 |
|------|------|----------|
| **云存储 (S3/GCS)** | 会话结束前将文件上传到云存储 | 需要保留完整项目文件 |
| **数据库存储** | 将代码内容存入数据库（如 Message 附件） | 小文件、代码片段 |
| **E2B Snapshot** | 使用 E2B 的快照功能保存整个环境状态 | 需要恢复完整运行环境 |
| **Git 仓库** | 将代码推送到用户的 Git 仓库 | 用户有自己的代码仓库 |

### Claudex 的实现方式

```python
# 会话结束时保存产物
async def save_sandbox_artifacts(sandbox: Sandbox, chat_id: str):
    # 1. 获取工作目录中的所有文件
    files = await sandbox.filesystem.list("/workspace")

    # 2. 上传到云存储
    for file in files:
        content = await sandbox.filesystem.read(file.path)
        await upload_to_s3(
            bucket="claudex-artifacts",
            key=f"{chat_id}/{file.path}",
            content=content
        )

    # 3. 在数据库中记录文件元数据
    await save_file_metadata(chat_id, files)

# 用户查看历史时恢复
async def restore_artifacts(chat_id: str, new_sandbox: Sandbox):
    # 从云存储下载并恢复到新 Sandbox
    files = await get_file_metadata(chat_id)
    for file in files:
        content = await download_from_s3(f"{chat_id}/{file.path}")
        await new_sandbox.filesystem.write(file.path, content)
```

### 对话历史中的代码展示

对于历史对话中的代码展示，有两种策略：

1. **实时存储**: CLI 执行过程中，将代码变更实时同步到数据库
2. **结束时快照**: 会话结束时，一次性保存所有文件

```
用户查看历史对话
        ↓
┌─────────────────────────────────────────────────────────┐
│  对话消息（来自数据库）                                   │
│  ├── User: "创建一个 React 应用"                         │
│  ├── Assistant: "好的，我来创建..."                      │
│  │     └── 工具调用: Write file src/App.tsx              │
│  │           └── 代码内容（已保存在消息附件中）            │
│  ├── Assistant: "已创建以下文件..."                      │
│  └── ...                                                │
└─────────────────────────────────────────────────────────┘
        ↓
    用户可选择
        ├── 仅查看代码 → 直接从数据库/存储读取
        └── 继续编辑 → 创建新 Sandbox 并恢复文件
```

**总结：E2B Sandbox 虽然是临时的，但通过在会话过程中或结束时将产物保存到持久化存储（云存储/数据库），可以实现历史数据的保留和恢复。**
